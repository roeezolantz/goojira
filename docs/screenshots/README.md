# Screenshots

Drop popover and settings screenshots here. Referenced from the root `README.md`.

Recommended:

- `popover-dark.png` — popover in dark theme, with a few sections populated.
- `popover-light.png` — same, light theme.
- `settings.png` — Settings window showing the Jira connection card.
- `demo.gif` — short loop (≤ 10 MB) showing tray click → popover → click an issue.

Keep individual PNGs under 1 MB. The GIF should be ≤ 10 MB so the README stays light.
